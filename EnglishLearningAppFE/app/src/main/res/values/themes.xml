<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Theme cơ bản -->
    <style name="Base.Theme.EnglishLearningApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Bạn có thể chỉnh màu chính ở đây -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Theme chính cho app -->
    <style name="Theme.EnglishLearningApp" parent="Base.Theme.EnglishLearningApp">
<!--        <item name="android:windowBackground">@color/white</item>-->
    </style>

    <!-- Theme riêng cho SplashActivity -->
    <style name="Theme.EnglishLearningApp.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/shape_background_splash</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
